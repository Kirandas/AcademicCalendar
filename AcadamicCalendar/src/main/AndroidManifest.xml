<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.academiccalendar"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calendar_icon"
        android:label="JCU ACADEMIC CALENDAR"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MonthView"
            android:label="JCU ACADEMIC CALENDAR">
        </activity>

        <activity
            android:name=".weekview.BasicActivity"
            android:label="JCU ACADEMIC CALENDAR" />
        <activity android:name=".trisem.TriSemAct" />

        <activity
            android:name=".color_keys"
            android:theme="@style/popup_window"
            />
        <activity
            android:name=".home_page"
            android:label="Academic Calendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
